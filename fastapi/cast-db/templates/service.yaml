apiVersion: v1
kind: Service
metadata:
  name: cast-db
  labels:
    app: cast-db
spec:
  selector:
    app: cast-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
